# 需求
1. 给 cheatsheet 添加快捷键 command + Shift + c 这个快捷键的功能是全局一键打开 App ，方便用户不需要通过启动台或者 Docker 栏点击打开
2. 打开之后后续的体验不用发生任何变化 

好的，我理解了你的需求。目标是为`cheatsheet`应用添加一个全局快捷键 `Command + Shift + C`，以便用户可以在任何地方快速唤出应用窗口。

这是一个典型的macOS应用增强功能，需要与系统的底层API进行交互。我将为你构建一个完整的变更提案。

# 提案 v1

## 1. 目标差分析 (Target Gap Analysis)

*   **[观测量] 代码现状评估：**
    当前应用是一个标准的SwiftUI应用，依赖于用户通过点击Dock图标、启动台或Spotlight等传统方式来启动和激活。代码库中没有处理全局键盘事件或系统级热键的机制。

*   **[目标量] 需求本质解读：**
    核心需求是提升应用的易用性和访问效率，让用户能够通过一个固定的、全局有效的快捷键（`Command + Shift + C`）立即将应用窗口带到前台，无论用户当前正在使用哪个应用。

*   **[目标差] 核心矛盾分析：**
    差距在于应用目前仅响应在其窗口活跃时发生的内部事件，而缺乏一个能够**监听系统全局键盘事件**并据此触发应用行为的机制。要弥合这一差距，必须引入与macOS底层事件处理系统（Carbon Event Manager）交互的代码。

## 2. 兼容性分析 (Compatibility Analysis)

本次变更将引入一个专门用于处理全局热键的服务类，该类将是高度内聚和独立的，对现有代码库的侵入性极低。

*   **精确“接驳点”：**
    新的 `GlobalHotkeyManager` 将在应用的生命周期入口 `cheatsheetApp.swift` 中被初始化并调用其注册方法。这是唯一的主动接驳点。
*   **数据兼容性：**
    不涉及任何数据模型（CoreData）的变更，无数据兼容性问题。
*   **函数调用栈：**
    新功能完全独立，不会改变现有的任何View、ViewModel或Model的函数调用关系。它通过系统回调来触发`NSApp.activate()`，这是一个标准的AppKit行为，不会与现有SwiftUI视图逻辑冲突。
*   **风险规避：**
    通过将所有与`Carbon.framework`相关的底层交互代码封装在单一的`GlobalHotkeyManager`类中，我们隔离了复杂性，确保了主应用逻辑的纯粹性和可维护性。该方案不会引入任何UI或业务逻辑上的兼容性风险。

## 3. 蓝图：技术方案 (Blueprint: Technical Solution)

我将设计一个名为`GlobalHotkeyManager`的单例服务来优雅地实现此功能。

1.  **创建 `GlobalHotkeyManager.swift`：**
    在 `cheatsheet/Utils/` 目录下创建一个新文件 `GlobalHotkeyManager.swift`。这个文件将负责所有与全局快捷键相关的逻辑。

2.  **实现 `GlobalHotkeyManager` 类：**
    *   该类将使用 `Carbon.framework` 中的API来注册一个系统级的快捷键。
    *   它会定义要注册的快捷键（`kVK_ANSI_C`，即'C'键）和修饰符（`cmdKey` + `shiftKey`）。
    *   使用 `RegisterEventHotKey` 函数向系统注册该快捷键。
    *   使用 `InstallEventHandler` 安装一个事件处理器，用于监听快捷键被按下的事件。
    *   当事件处理器接收到快捷键事件时，它将在主线程上调用 `NSApp.activate(ignoringOtherApps: true)`，这个方法可以强制激活我们的应用并将其窗口前置。

3.  **集成到应用生命周期：**
    *   在 `cheatsheetApp.swift` 的主 `App` 结构体中，添加一个 `init()` 方法。
    *   在 `init()` 方法中，调用 `GlobalHotkeyManager.shared.register()` 来启动快捷键的监听。这确保了应用一启动，快捷键就处于可用状态。

**方案优势：**
*   **高内聚、低耦合：** 将复杂的Carbon API调用完全封装，对现有SwiftUI代码零侵入。
*   **职责单一：** `GlobalHotkeyManager`只做一件事：管理全局快捷键。
*   **生命周期安全：** 在应用启动时注册，随着应用退出，系统会自动处理资源的释放，逻辑清晰。

## 4. 蓝图：上下文引用 (Blueprint: Context References)
<relevant_context_files>
cheatsheet/cheatsheetApp.swift
</relevant_context_files>

## 5. 蓝图：Diff明细 (Blueprint: Diff Detail)

我将创建一个新文件 `cheatsheet/Utils/GlobalHotkeyManager.swift` 并修改 `cheatsheet/cheatsheetApp.swift`。

**`cheatsheet/Utils/GlobalHotkeyManager.swift` (new file)**
```swift
//
//  GlobalHotkeyManager.swift
//  cheatsheet
//
//  Created by 周麒 on 2025/8/10.
//

import Foundation
import Carbon
import AppKit

class GlobalHotkeyManager {

    static let shared = GlobalHotkeyManager()

    private var hotKeyRef: EventHotKeyRef?
    private var eventHandler: EventHandlerRef?

    private init() {}

    func register() {
        // 定义快捷键ID
        let hotKeyID = EventHotKeyID(signature: "cht1".fourChar(), id: 1)

        // 定义快捷键组合：Command + Shift + C
        // kVK_ANSI_C is the key code for 'C'
        let keyCode = UInt32(kVK_ANSI_C)
        // cmdKey and shiftKey are modifier flags
        let modifiers = UInt32(cmdKey | shiftKey)

        // 1. 注册全局快捷键
        var gMyHotKeyRef: EventHotKeyRef?
        let status = RegisterEventHotKey(keyCode, modifiers, hotKeyID, GetApplicationEventTarget(), 0, &gMyHotKeyRef)
        if status != noErr {
            print("Error: Unable to register hotkey, status: \(status)")
            return
        }
        self.hotKeyRef = gMyHotKeyRef

        // 2. 安装事件处理器以监听快捷键事件
        var eventType = EventTypeSpec(eventClass: OSType(kEventClassKeyboard), eventKind: OSType(kEventHotKeyPressed))
        let installStatus = InstallEventHandler(GetApplicationEventTarget(), {
            (handlerRef, eventRef, userData) -> OSStatus in
            // 将 self (GlobalHotkeyManager instance) 从 userData 中恢复
            let manager = Unmanaged<GlobalHotkeyManager>.fromOpaque(userData!).takeUnretainedValue()
            return manager.handleHotKeyEvent(eventRef)
        }, 1, &eventType, Unmanaged.passUnretained(self).toOpaque(), &eventHandler)

        if installStatus != noErr {
            print("Error: Unable to install event handler, status: \(installStatus)")
        }
    }

    private func handleHotKeyEvent(_ eventRef: EventRef?) -> OSStatus {
        guard let eventRef = eventRef else { return OSStatus(eventNotHandledErr) }

        var hotKeyID = EventHotKeyID()
        let status = GetEventParameter(
            eventRef,
            UInt32(kEventParamDirectObject),
            UInt32(typeEventHotKeyID),
            nil,
            MemoryLayout<EventHotKeyID>.size,
            nil,
            &hotKeyID
        )

        if status != noErr {
            return status
        }
        
        // 检查是否是我们注册的快捷键
        if hotKeyID.signature == "cht1".fourChar() && hotKeyID.id == 1 {
            // 在主线程激活应用
            DispatchQueue.main.async {
                NSApp.activate(ignoringOtherApps: true)
                // 如果有窗口，可以尝试让窗口成为 key window
                if let window = NSApp.windows.first {
                    window.makeKeyAndOrderFront(nil)
                }
            }
            return noErr
        }
        
        return OSStatus(eventNotHandledErr)
    }

    func unregister() {
        if let hotKeyRef = self.hotKeyRef {
            UnregisterEventHotKey(hotKeyRef)
            self.hotKeyRef = nil
        }
        if let eventHandler = self.eventHandler {
            RemoveEventHandler(eventHandler)
            self.eventHandler = nil
        }
    }
}

// Helper to convert a 4-character string to a FourCharCode (OSType)
extension String {
    func fourChar() -> FourCharCode {
        return self.utf16.reduce(0, {$0 << 8 + FourCharCode($1)})
    }
}

```

**`cheatsheet/cheatsheetApp.swift`**
```diff
 //
 //  cheatsheetApp.swift
 //  cheatsheet
 //
 //  Created by 周麒 on 2025/6/9.
 //
 
 import SwiftUI
 
 @main
 struct cheatsheetApp: App {
     let persistenceController = PersistenceController.shared
+
+    init() {
+        GlobalHotkeyManager.shared.register()
+    }
 
     var body: some Scene {
         WindowGroup {

```

## 6. 闭环验证 (Closed-Loop Verification)

我已审视上述变更。
1.  创建 `GlobalHotkeyManager.swift` 文件并添加代码。
2.  在 `cheatsheetApp.swift` 中应用 `diff` 修改。
编译并运行应用后，`GlobalHotkeyManager`的`register`方法将被调用，`Command + Shift + C`快捷键会立即生效。无论应用是否在前台，按下此快捷键都会调用`NSApp.activate`，将应用窗口带到最前。
此方案是完整且独立的，应用此`diff`后即可实现所需功能，无需对其他任何文件进行改动。

## 7. 预期收益 (Expected Outcome)

*   **提升效率**：用户可以从任何工作上下文（如编写代码、浏览文档）中通过快捷键瞬间唤出应用，极大提升了命令的查找和复制效率。
*   **改善体验**：将`cheatsheet`从一个需要主动寻找的应用，转变为一个常驻后台、随时可用的高效工具，符合此类“备忘录”/“工具箱”应用的核心使用场景。
*   **专业性增强**：全局快捷键是专业级工具应用的标志性功能之一，此项改进能有效提升产品的专业形象。

## 8. 归档信息 (Archival Information)

feat(core): 实现全局快捷键 Command+Shift+C 唤出应用